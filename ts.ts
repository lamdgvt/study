/**
 * let 变量: 类型;
 * let 变量: 类型 = 值
 * 
 * boolean 类型
 * number 数字
 * string 字符串
 * 
 * 数组
 * number[] = [1, 2, 3, 4]
 * Array<number> = [1, 2, 3]
 * 
 * Tuple
 * let x: [string, number]
 * 
 * enum 枚举
 * enum Color {Red, Green, Blue}
 * let c: Color = Color.Green;
*/

let action: string = '123';

enum Color {Red, Green, Blue}
let c: Color = Color.Green;

