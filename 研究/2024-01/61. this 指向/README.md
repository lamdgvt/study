#### this 指向规则

```
1. 默认绑定规则
    正常函数里 this 指向一般是 window

2. 隐式绑定规则
    对象内的函数内, this 一般谁调用指向谁

3. 显示绑定规则
    call、bind、apply, 改变 this 指向绑定

4. new 绑定
    this 一般指向砬对象本身 不能使用箭头函数


箭头函数不存在 this, 所以 箭头函数的 this 是父级作用域的 this.

优先级：
new绑定优先级 > 显示绑定优先级 > 隐式绑定优先级 > 默认绑定优先级

```