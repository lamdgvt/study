### Linux 命令

```
cd
pwd
ls
mkdir 创建文件夹
rmdir 删除文件夹
wget https://.... 下载


tar tar文件解压工具
tar -zxvf   

-z:是否同时具有gzip的属性？即是否需要用gzip压缩或解压？一般格式为xxx.tar.gz或xx.tgz
-x: --extract,--get解压文件
-v:显示操作过程！这个参数很常用
-f：使用文档名，注意，在f之后要立即接文档名，不要再加其他参数！

```
